services:
  - type: web
    name: pocketbase-monitor
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
    disk:
      name: pb-data
      mountPath: /pb_data
      sizeGB: 1